"""Evaluation entry point placeholder.

The primary training command already performs strict held-out target evaluation
using the best source-validation checkpoint. A standalone checkpoint evaluator
will be added when the final checkpoint metadata format is frozen.
"""

if __name__ == "__main__":
    raise SystemExit("Use scripts/train.py for the v0.1 strict zero-shot protocol.")
